{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "devops-platform.serviceAccountName" . }}
  labels:
    {{- include "devops-platform.labels" . | nindent 4 }}
{{- end }}
