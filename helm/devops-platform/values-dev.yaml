global:
  environment: dev

replicaCount: 1

image:
  tag: "latest"

resources:
  requests:
    cpu: "50m"
    memory: "64Mi"
  limits:
    cpu: "200m"
    memory: "256Mi"

autoscaling:
  enabled: false

ingress:
  host: api.devops.dev.local

env:
  NODE_ENV: development
  APP_VERSION: "2.0.0-dev"
